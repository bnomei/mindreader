interface:
  display_name: "Using Mindreader"
  short_description: "Autonomously recall and maintain agent memory"
  default_prompt: "Use $using-mindreader as selective durable graph memory: recall stored context when it can affect this task and capture only supported future-useful assertions you deliberately choose."
